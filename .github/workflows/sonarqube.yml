name: CI Python & SonarQube Analysis

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-analyze:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🛎️ Checkout code
        uses: actions/checkout@v4
        with:
          # Importante: se requiere el historial completo para el análisis de SonarQube
          fetch-depth: 0 

      - name: 🛠️ Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          # Usar la versión recomendada por tus requerimientos
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Instala todas las dependencias del proyecto, incluyendo pytest y pytest-cov
          pip install -r requirements.txt 

      # Tarea I: Compilación (en Python es la instalación) y Ejecución de Pruebas
      - name: 🧪 Run Tests & Generate Reports
        run: |
          # 1. Ejecutar pruebas (pytest) y generar dos reportes en formato XML:
          #    - coverage.xml (para la Cobertura de Código)
          #    - junit.xml (para los Resultados de las Pruebas)
          # NOTA: Asegúrate de tener el plugin necesario, como pytest-junitxml, si pytest-cov no lo genera automáticamente.
          pytest --cov-report=xml --cov=. --junitxml=junit.xml 
          
          # 2. Ejecutar el análisis de estilo de código (Linting)
          flake8 . --output-file=flake8_report.txt --exit-zero

# Tarea II: Análisis Estático con SonarQube
      - name: 🔍 SonarQube Analysis
        uses: SonarSource/sonarcloud-github-action@v2.2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          args: >
            -Dsonar.projectKey=Yaelgpt_pruebaaaaaaaaaaaaa 
            -Dsonar.organization=Yaelgpt 
            -Dsonar.sources=.
            -Dsonar.python.coverage.reportPaths=coverage.xml
            -Dsonar.python.xunit.reportPaths=junit.xml
            # ELIMINA CUALQUIER OTRA LÍNEA DE CONFIGURACIÓN AQUÍ
